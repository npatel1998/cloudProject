type Item {
  pk: ID!
  sk: ID!
  name: String!
  description: String
}

type Query {
  getItem(pk: ID!, sk: ID!): Item
  listItems: [Item]
}

type Mutation {
  addItem(pk: ID!, sk: ID!, name: String!, description: String): Item
}