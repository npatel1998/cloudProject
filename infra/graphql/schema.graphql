type Item {
  id: ID!
  name: String
  description: String
}

type Query {
  getItem(id: ID!): Item
  listItems: [Item]
}

type Mutation {
  addItem(id: ID!, name: String, description: String): Item
}